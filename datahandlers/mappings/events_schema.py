"""
this module is for the schema mapping
pandas DataFrame loads some columns with dtype
as 'object', this needs to be corrected for the
pandas.DataFrame.to_sql call to load DataFrame
into sqllite db
"""

EVENTSSCHEMA={
}
